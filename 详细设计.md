# 会场大屏播放控制软件 详细设计

## 1. 文档概述
本文档详细描述系统的技术实现方案，包括代码架构、关键设计决策、模块内部设计、数据流、线程模型和错误处理策略。适用于开发人员和代码审查人员理解系统的实现细节。

## 2. 技术架构详解

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────┐
│                     Application                      │
│               App.xaml.cs (启动入口)                  │
├─────────────────────────────────────────────────────┤
│                   表示层 (XAML)                       │
│  ┌──────────────────┐  ┌─────────────────────────┐  │
│  │   MainWindow      │  │     ShowWindow           │  │
│  │   (中控台)         │  │     (展示窗口)           │  │
│  │   940x680         │  │     960x540              │  │
│  │   ResizeMode:     │  │     WindowStyle: None    │  │
│  │   CanResize       │  │     ResizeMode: Grip     │  │
│  └────────┬─────────┘  └────────────┬────────────┘  │
│           │ DataContext              │ 方法调用      │
│           ▼                         ▼              │
├─────────────────────────────────────────────────────┤
│                 ViewModel 层                        │
│  ┌──────────────────────────────────────────────┐   │
│  │            MainViewModel                      │   │
│  │  - INotifyPropertyChanged                     │   │
│  │  - ICommand 实现 (RelayCommand)               │   │
│  │  - 属性绑定 (播放状态/进度/音量/设置)         │   │
│  │  - 事件订阅 (ItemStarted, PlaybackStopped)    │   │
│  │  - DispatcherTimer (250ms 视频进度轮询)        │   │
│  └──────────────────┬───────────────────────────┘   │
│                     │ 方法调用/事件                    │
│                     ▼                                │
├─────────────────────────────────────────────────────┤
│                 Services 层                          │
│  ┌──────────┐ ┌───────────────┐ ┌──────────────┐   │
│  │Playlist  │ │ Playback      │ │ FileValidator│   │
│  │Manager   │ │ Controller    │ │              │   │
│  └────┬─────┘ └───────┬───────┘ └──────────────┘   │
│       │               │                             │
│       ▼               ▼                             │
├─────────────────────────────────────────────────────┤
│               Infrastructure 层                      │
│  ┌──────────────┐ ┌────────────────┐ ┌──────────┐  │
│  │VlcMediaPlayer│ │Playlist        │ │  Logger  │  │
│  │(LibVLCSharp) │ │Persistence     │ │          │  │
│  │              │ │(JSON/AppData)   │ │          │  │
│  └──────────────┘ └────────────────┘ └──────────┘  │
└─────────────────────────────────────────────────────┘
```

### 2.2 依赖注入方式
项目不使用 IoC 容器，采用**手动依赖注入**。所有依赖在 `App.OnStartup()` 中按顺序创建并注入：

```csharp
PlaylistPersistence → PlaylistManager / AppSettings
FileValidator       → PlaylistManager
PlaylistManager     → PlaybackController / MainViewModel
VlcMediaPlayer      → PlaybackController / ShowWindow
PlaybackController  → MainViewModel
AppSettings         → MainViewModel
ShowWindow          → App (事件绑定) / MainViewModel
```

### 2.3 跨线程模型
- **UI 线程**：WPF Dispatcher 线程，处理所有 UI 更新。
- **视频轮询**：`DispatcherTimer`（250ms），在 UI 线程上触发 Tick。
- **VLC 事件**：`EndReached` 等事件在**后台线程**触发，需要通过 `Dispatcher.Invoke()` 回到 UI 线程。
- **音乐淡入/淡出**：`System.Timers.Timer`（100ms），在后台线程修改 `Volume` 属性（LibVLC 线程安全）。
- **持久化 IO**：同步执行（文件读写操作较快，暂时异步化需求不大）。

## 3. 模块详细设计

### 3.1 App.xaml.cs — 应用入口

**作用**：依赖注入容器和启动引导。

**关键代码路径**：
```
OnStartup():
  1. 创建 PlaylistPersistence → LoadSettings() → AppSettings
  2. ThemeManager.Initialize(IsDarkTheme)
  3. 创建 FileValidator
  4. 创建 PlaylistManager(注入 persistence, fileValidator)
  5. 创建 VlcMediaPlayer → 设置视频音量
  6. 创建 PlaybackController(注入 playlistManager, mediaPlayer)
  7. 创建 ShowWindow → SetMediaPlayer, SetTransitionConfig, Show()
  8. 创建 MainViewModel(注入所有依赖)
  9. 注册 PlaybackController.ItemStarted 事件:
     → ShowWindow.EnsureVisible()
     → ShowWindow.SetTransitionConfig()
     → ShowWindow.TransitionTo(item)
  10. 创建 MainWindow(注入 viewModel) → MainWindow = this → Show()
```

**设计决策**：
- ShowWindow 先于 MainWindow Show，确保展示窗口先出现。
- 显式设置 `MainWindow = mainWindow` 和 `ShutdownMode = OnMainWindowClose`，确保中控台关闭时整个应用退出。
- ItemStarted 事件在 App.xaml.cs 中注册（而不是 ViewModel），因为 ShowWindow 是应用级资源。

### 3.2 MainWindow.xaml.cs — 中控台窗口

**作用**：用户交互的主界面，负责所有控制操作。

**代码事件处理**：

| 事件 | 处理 | 功能 |
|------|------|------|
| `PreviewKeyDown` | `_viewModel.HandleKeyDown()` | 全局快捷键 |
| `Slider.PreviewMouseDown` | `_viewModel.IsDraggingSlider = true` | 开始拖拽进度条 |
| `Slider.PreviewMouseUp` | `_viewModel.SeekVideoCommand.Execute()` | 结束拖拽并跳转 |
| `Slider.ValueChanged` | 拖拽中更新 `VideoTimeText` | 实时预览时间 |
| `ListBox.PreviewMouseLeftButtonDown` | 记录拖拽起点 | 拖拽排序 |
| `ListBox.PreviewMouseMove` | `DragDrop.DoDragDrop()` | 启动拖拽 |
| `ListBox.Drop` | `_viewModel.OnItemDragDrop()` | 完成拖拽排序 |
| `Window.Closing` | `_viewModel.SaveAll()` | 退出保存 |

**拖拽排序实现细节**：
```csharp
// 1. MouseDown 记录起点
_dragStartPoint = e.GetPosition(null);
_isDragging = false;

// 2. MouseMove 检测是否超过拖拽阈值
var diff = _dragStartPoint - pos;
if (|diff.X| > MinDragDistance || |diff.Y| > MinDragDistance)
    DragDrop.DoDragDrop(listBox, selectedItem, DragDropEffects.Move);

// 3. Drop 计算目标索引
for (int i = 0; i < listBox.Items.Count; i++)
{
    var item = ContainerFromIndex(i) as ListBoxItem;
    var bounds = item.TransformToAncestor(listBox).TransformBounds(...);
    if (dropPosition.Y < bounds.Top + bounds.Height / 2)
        return i;  // 插入到该项之前
}
return listBox.Items.Count - 1;  // 默认末尾
```

### 3.3 MainViewModel — 主视图模型

**作用**：应用中控台核心逻辑中枢。

#### 核心属性分离设计

```csharp
// 列表选中项 — 用户在列表中单击选择的项
PlaylistItem? CurrentItem
  → 驱动右侧"选中项"面板（循环、音乐设置）

// 当前播放项 — 实际在展示窗口播放的项
PlaylistItem? NowPlayingItem
  → 驱动列表 ▶ 标记、播放控制区域、顶部文件名
```

**设计理由**：用户可能选中一个项目来配置它的循环/音乐，但播放的可能是另一个项目。分开后：
- 选中项永远指向列表的高亮行。
- 播放项指向实际播放的内容。
- "从此项开始播放"按钮仅在两者不同时显示。
- 列表中的 ▶ 标记由 `PlaylistItem.IsNowPlaying` 属性驱动。

#### 属性变化通知

```csharp
CurrentItem setter → 通知: IsSelectedItemVideo, IsSelectedItemImage,
                     SelectedMusicPath, SelectedHasMusic, SelectedVideoLoop,
                     SelectedMusicLoop, SelectedMusicFileName,
                     SelectedItemDisplayName, ShowSelectedPlayButton

NowPlayingItem setter → 通知: ShowNowPlayingInfo, NowPlayingDisplayName,
                        NowPlayingTypeText, IsNowPlayingVideo,
                        IsNowPlayingImage, ShowSelectedPlayButton,
                        + 自动清进度条
```

#### 命令映射

| ICommand | 绑定方法 | 对应 PlaybackController 方法 |
|----------|----------|------------------------------|
| `AddFilesCommand` | `OnAddFiles()` | —（文件对话框→PlaylistManager.Add）|
| `RemoveItemCommand` | `OnRemoveItem()` | —（PlaylistManager.Remove）|
| `ClearAllCommand` | lambda | `Stop()` + PlaylistManager.Clear() |
| `PlayItemCommand` | `OnPlayItem()` | `PlayItem(item)` |
| `NextCommand` | lambda | `Next()` |
| `PreviousCommand` | lambda | `Previous()` |
| `StopCommand` | lambda | `Stop()` |
| `PauseResumeCommand` | `OnPauseResume()` | `PauseVideo()` / `ResumeVideo()` |
| `ToggleVideoLoopCommand` | `OnToggleVideoLoop()` | 修改 CurrentItem.IsVideoLoop |
| `SelectMusicCommand` | `OnSelectMusic()` | 文件对话框→CurrentItem.MusicPath |
| `ClearMusicCommand` | `OnClearMusic()` | CurrentItem.MusicPath = null |
| `ToggleMusicLoopCommand` | `OnToggleMusicLoop()` | 修改 CurrentItem.IsMusicLoop |
| `SeekVideoCommand` | `OnSeekVideo()` | `SeekVideo()` |
| `MoveItemCommand` | `OnMoveItem()` | `RefreshCurrentIndex()` |
| `ToggleShowWindowCommand` | lambda | ShowWindow.EnsureVisible/Minimize |
| `SelectIdleImageCommand` | `OnSelectIdleImage()` | 文件对话框→AppSettings |

#### 视频进度轮询

```csharp
_positionTimer = new DispatcherTimer { Interval = TimeSpan.FromMilliseconds(250) };
_positionTimer.Tick += (s, e) =>
{
    if (_isDraggingSlider) return;  // 拖拽时暂停自动更新
    var pos = _playbackController.GetVideoTime();
    var len = _playbackController.GetVideoLength();
    if (len > 0)
    {
        VideoProgress = Math.Min(1.0, (double)pos / len);
        VideoTimeText = FormatTime(pos);
        VideoDurationText = FormatTime(len);
    }
};
```

### 3.4 ShowWindow.xaml.cs — 展示窗口

**作用**：独立于中控的展示窗口，负责媒体显示和转场动画。

#### 状态管理

```csharp
private bool _isFullscreen;         // 全屏状态
private double _previousLeft, ...;   // 全屏前位置尺寸（用于恢复）
private bool _isTransitioning;       // 转场中锁（防止重复触发）
private TransitionType _transitionType;  // 当前转场配置
private int _transitionDurationMs;       // 转场时长
```

#### 转场动画实现

两种模式：

**Fade（淡入淡出）**：
```
FadeOverlay Opacity 0→1 (duration)
  → 动画完成回调: ClearContent() + LoadContent(item)
    → FadeOverlay Opacity 1→0 (duration)
      → 完成: Opacity = 0, _isTransitioning = false
```

**Crossfade（直接切换）**：
```
FadeOverlay.Opacity = 0（透明，不做动画）
ClearContent() + LoadContent(item)（直接替换内容）
_isTransitioning = false（立即解锁）
```

注意：Crossfade 模式下，新旧内容切换瞬间完成，不做黑场过渡。

#### 内容加载

```csharp
private void LoadContent(PlaylistItem item)
{
    if (item.Type == MediaType.Image)
    {
        ContentVideo.Visibility = Collapsed;
        ContentImage.Visibility = Visible;
        // 加载 BitmapImage 并设置 Source
        var bitmap = new BitmapImage();
        bitmap.BeginInit();
        bitmap.CacheOption = BitmapCacheOption.OnLoad;  // 立即加载到内存
        bitmap.UriSource = new Uri(item.FilePath);
        bitmap.EndInit();
        ContentImage.Source = bitmap;
    }
    else  // Video
    {
        ContentImage.Visibility = Collapsed;
        ContentVideo.Visibility = Visible;
        _mediaPlayer?.PlayVideo(item.FilePath, item.IsVideoLoop);
    }
}
```

#### 窗口生命周期管理

```
✕ 按钮  → WindowState = Minimized （最小化）
Alt+F4  → OnClosing: Cancel = true, Minimized
系统菜单关闭 → OnClosing: Cancel = true, Minimized

EnsureVisible():
  if (!IsVisible) Show()     // 如果已被隐藏（极少情况）
  if (Minimized) Normal
  Activate()
```

设计要点：展示窗口**永不关闭**，只最小化。OnClosing 拦截所有关闭操作，转换为最小化。如果中控台关闭（MainWindow 关闭），则 `ShutdownMode.OnMainWindowClose` 触发应用退出，所有窗口包括 ShowWindow 都会自动关闭。

### 3.5 PlaybackController — 播放控制器

**作用**：协调播放列表和媒体引擎，驱动播放流程。

#### 索引管理

PlaybackController 并行维护两种索引：
- `_playingItem`：当前播放对象的直接引用（不依赖索引查找）
- `_currentIndex`：当前播放项的列表索引（用于 Next/Previous 导航）

**Next() 边界行为**：
```csharp
Next():
  nextIndex = _currentIndex + 1

  if (nextIndex >= Items.Count):
    if (_currentIndex >= Items.Count):
      // 已处于末尾停止状态，再点 Next → 回到索引 0 播放
      nextIndex = 0
    else:
      // 刚到达末尾 → 停止
      _currentIndex = Items.Count
      _playingItem = null
      StopCurrent()
      PlaybackStopped.Invoke()
      return

  PlayItem(nextIndex)
```

**拖拽排序后索引刷新**：
```csharp
RefreshCurrentIndex():
  if (_playingItem != null):
    newIndex = playlistManager.Items.IndexOf(_playingItem)
    if (newIndex >= 0):
      _currentIndex = newIndex
    // 如果 newIndex < 0（被删除了），_currentIndex 保持不变，
    // 但下一次 Next() 会触发 Stop()（索引越界）
```

#### 事件定义

```csharp
event EventHandler<PlaylistItem> ItemStarted    // 项目开始播放
event EventHandler PlaybackStopped               // 播放停止
event EventHandler MediaEnded                    // VLC 媒体播放结束
```

### 3.6 VlcMediaPlayer — 媒体引擎封装

**作用**：封装 LibVLCSharp，提供应用层友好的媒体控制 API。

#### 播放器实例

| 实例 | 用途 | 生命周期 |
|------|------|----------|
| `_videoPlayer` (主MediaPlayer) | 视频播放、图片背景音乐播放 | 应用全程存在 |
| `_musicPlayer` (独立MediaPlayer) | 背景音乐播放 | 按需创建和销毁 |

#### 视频播放

```csharp
PlayVideo(filePath, loop):
  StopVideo()
  media = new Media(_libVlc, filePath, FromType.FromPath)
  if (loop): media.AddOption("--input-repeat=-1")  // VLC 无限循环
  _videoPlayer.Play(media)
  media.Dispose()  // 播放开始后 media 可释放
```

循环通过 VLC 内部选项实现，比监听 EndReached 再重新 Play 更可靠（无缝衔接）。

#### 背景音乐管理

**淡入**：3 秒（30 步 × 100ms）
```csharp
StartFadeIn():
  _musicPlayer.Volume = 0
  for step in 1..30:
    每 100ms → Volume += 100/30
    到达 100 → 停止 Timer
```

**淡出**：3 秒（30 步 × 100ms）
```csharp
StartFadeOut(onComplete):
  startVolume = _musicPlayer.Volume
  decrement = startVolume / 30
  for step in 1..30:
    每 100ms → Volume -= decrement
    <= 0 → Stop(), Cleanup(), onComplete()
```

**音乐循环**：通过监听 `EndReached` 事件重新 Play。
```csharp
_musicPlayer.EndReached += OnMusicEndReached;
// OnMusicEndReached 中重新调用 PlayMusic()（如果不停止）
```

#### 线程安全

- `_videoPlayer.Volume` 和 `_musicPlayer.Volume` 可以在后台线程安全调用（LibVLC 内部有锁）。
- 但 `EndReached` 事件在后台线程触发，处理中调用 `Next()`（最终到 UI 线程）或重新 `Play()` 需要 `Dispatcher.Invoke()`。

### 3.7 PlaylistManager — 列表管理

**核心数据结构**：
```csharp
private readonly ObservableCollection<PlaylistItem> _items = [];
public ReadOnlyObservableCollection<PlaylistItem> Items { get; }
```

- `ObservableCollection` 提供自动 UI 更新（添加、删除、排序触发 CollectionChanged）。
- 对外暴露 `ReadOnlyObservableCollection`，防止外部直接修改。
- 修改方法（Add、Remove、Move、Clear）在内部调用 `_items` 操作。

#### 持久化触发

`PlaylistManager` 不自动触发保存。外部在适当时机调用：
- `MainWindow.OnClosing` → `_viewModel.SaveAll()` → `_playlistManager.SaveToPersistence()`
- 每次重要操作后（添加/删除/排序）可能需要即时保存？当前仅在退出时保存。

### 3.8 PlaylistPersistence — JSON 持久化

#### 文件位置
```
%AppData%\ShowPlayer\
  ├── playlist.json     # 播放列表
  └── settings.json     # 应用设置
```

#### playlist.json 格式
```json
{
  "Items": [
    {
      "Id": "guid",
      "Type": "Image/Video",
      "FilePath": "D:\\assets\\logo.png",
      "MusicPath": null,
      "IsMusicLoop": false,
      "IsVideoLoop": true,
      "IsMissing": false
    }
  ]
}
```

#### settings.json 格式
```json
{
  "TransitionType": "Fade/Crossfade",
  "TransitionDurationMs": 500,
  "FallbackImagePath": null,
  "FallbackVideoPath": null,
  "IsDarkTheme": false,
  "VideoVolume": 100,
  "MusicVolume": 100,
  "DefaultIdleImagePath": null
}
```

#### 序列化配置
```csharp
new JsonSerializerOptions
{
    WriteIndented = true,
    Converters = { new JsonStringEnumConverter() }  // 枚举序列化为字符串
};
```

### 3.9 Converters — 值转换器

| 转换器 | 输入 | 输出 | 用途 |
|--------|------|------|------|
| MissingBorderConverter | bool | RedBrush / TransparentBrush | 文件缺失边框 |
| MissingForegroundConverter | bool | RedBrush / BlackBrush | 文件缺失字体（未使用） |
| StringToVisibilityConverter | string? | Collapsed / Visible | 音乐文件名存在性 |
| BoolToVisibilityConverter | bool | Collapsed / Visible | 通用 true→显示 |
| NullToVisibilityConverter | object? | Visible / Collapsed | null→显示（无内容提示） |
| NotNullToVisibilityConverter | object? | Collapsed / Visible | 非 null→显示 |
| InverseBoolToVisibilityConverter | bool | Visible / Collapsed | false→显示 |
| InverseBoolConverter | bool | !bool | RadioButton 取反绑定 |

## 4. 关键设计决策详解

### 4.1 选中 vs 播放分离
```
用户单击列表 → CurrentItem 更新 → 右侧面板显示选中项配置
用户双击/点播放 → PlaybackController 播放 → NowPlayingItem 更新
  → 列表 ▶ 标记指向 NowPlayingItem
  → "播放控制"区域显示 NowPlayingItem 的信息
  → 顶部显示 NowPlayingItem 文件名
```

### 4.2 后台线程事件处理
VLC 的 `EndReached` 事件在 VLC 内部线程触发，不能直接操作 UI。处理链：
```
VLC 内部线程: EndReached
  → PlaybackController.OnMediaEnded (后台线程)
    → MediaEnded.Event (后台线程)
      → MainViewModel.OnMediaEnded (后台线程)
        → Dispatcher.Invoke(() => PlaybackController.Next())
          → UI 线程执行 Next() → 触发 ItemStarted → UI 线程更新
```

### 4.3 展示窗口关闭保护
ShowWindow 的关闭保护机制防止用户意外关闭导致播放中断：
```
用户 Alt+F4 / 系统菜单关闭
  → OnClosing(CancelEventArgs e)
    → 检查: 如果 MainWindow 还在运行
      → e.Cancel = true, WindowState = Minimized（取消关闭，改为最小化）
    → 如果 MainWindow 已关闭（应用退出时）
      → 不拦截，允许正常关闭
```

### 4.4 默认闲置图片
```
用户设置 → AppSettings.DefaultIdleImagePath
  → 持久化到 settings.json

触发时机:
  PlaybackController.Stop()
    → PlaybackStopped 事件
      → MainViewModel.OnPlaybackStopped()
        → _showWindow.ShowDefaultImage(_appSettings.DefaultIdleImagePath)
          → ClearContent() + 加载并显示图片

  PlaybackController.Next() 到达末尾
    → PlaybackStopped 事件（同上）
```

## 5. 错误处理策略

### 5.1 文件缺失
```
加载/添加时 → FileValidator 检查 File.Exists()
  → PlaylistItem.IsMissing = true/false
  → UI: XAML DataTrigger → 红色字体 + 边框

播放时:
  PlaybackController.PlayItem():
    if (item.IsMissing) → Logger.Warning + return（跳过）
```

### 5.2 图片加载失败
```csharp
LoadContent(item):
  try:
    BitmapImage → 加载
  catch:
    ContentImage.Source = null（显示空白）
```

### 5.3 持久化异常
```csharp
Save() / Load():
  try-catch → Logger.Error()
  Load 失败 → 返回空列表 / 默认设置
```

### 5.4 跨线程异常
```csharp
OnMediaEnded(): try { Dispatcher.Invoke(...) } catch { }
OnPlaybackStopped(): Dispatcher.Invoke(...)
// 应用退出时 Dispatcher 可能已不可用，try-catch 静默忽略
```

### 5.5 VLC 初始化失败
当前未做特殊处理。VLC 初始化在构造函数完成，如果创建失败（例如找不到 `libvlc` 原生库），应用在启动时就会崩溃。这是故意设计的——无法播放就立即失败，不留无声/黑屏的静默错误。

## 6. 构建与部署

### 6.1 调试构建
```powershell
dotnet build --nologo
```

### 6.2 发布构建
```powershell
dotnet publish --configuration Release --runtime win-x64 --self-contained true
```

### 6.3 发布输出结构
```
bin\publish\
├── ShowPlayer.App.exe          # 应用入口
├── *.dll                       # 运行时和依赖
├── libvlc\                     # VLC 原生库
│   ├── libvlc.dll
│   ├── libvlccore.dll
│   └── plugins\                # VLC 插件
```

## 7. 已知技术限制和潜在改进

### 7.1 当前限制
- 视频进度条使用 250ms 轮询而非 VLC 的事件回调，精度一般。
- 图片采用 `BitmapCacheOption.OnLoad` 立即内存加载，大图片可能卡 UI。
- 无独立的媒体结束自动切换逻辑（视频和图片用同一个 `MediaEnded` 事件，图片无意义）。
- 音乐淡出后不能再淡入（`_musicPlayer` 被 Dispose）。
- `FallbackImagePath` / `FallbackVideoPath` 字段已定义但功能未实现。
- 无快捷键帮助界面（`ShowShortcuts()` 使用 MessageBox 而非内嵌面板）。
- 图片的 `MusicPath` 配置修改后不会自动重启音乐（需用户手动停止再播放）。

### 7.2 潜在改进方向
- 使用 `BindingList<PlaylistItem>` 替代手动 `NotifyAllProperties`。
- 引入 `Microsoft.Toolkit.Mvvm`（CommunityToolkit）简化 ViewModel 代码生成。
- 将 VLC 事件处理中的 `Dispatcher.Invoke` 统一封装到线程安全适配器。
- 设置界面增加主题切换开关。
- 为 ListBox 添加虚拟化 UI 虚拟化优化（已启用 `VirtualizingPanel.ScrollUnit="Pixel"`）。
- 增加异步图片加载（`BitmapCacheOption.OnLoad` 改为后台线程解码）。
