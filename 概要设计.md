
 # 会场大屏播放控制软件 概要设计

## 1. 引言
本文档为该软件的总体技术架构设计，覆盖技术选型背景、MVP版本定义、系统模块划分、数据流和关键交互逻辑。  
项目采用 **Trae IDE + DeepSeek V4** 作为开发环境，目标部署为 Windows 10/11 上的绿色免安装应用。开发策略为先构建 MVP 验证核心流程，再迭代完善。

## 2. 技术选型与决策

### 2.1 应用框架：WPF + .NET 8
| 方案 | 主要优势 | 主要劣势 | 结论 |
|------|----------|----------|------|
| **A. WPF (.NET 8)** | 原生 Windows 性能佳；多窗口控制成熟；XAML 布局灵活；LibVLCSharp 配套完善；自包含发布后零依赖。 | 需 C# 技能，Web 前端开发者有转换成本。 | **选中** |
| B. Electron + Web前端 | 前端生态丰富，跨平台。 | 内存占用大，视频硬解和本地格式兼容难保证；多屏全屏控制复杂；体积大。 | 不选 |
| C. Qt (C++/Python) | 性能高，跨平台。 | C++开发效率低；Python绑定的打包和性能稍弱；媒体集成方案不如 VLC 完善。 | 不选 |

**决策理由**：WPF 是 Windows 桌面开发的首选原生框架，配合 .NET 8 可实现自包含部署，彻底消除对 .NET 运行时的外部依赖，真正做到绿色开箱即用。

### 2.2 媒体引擎：LibVLCSharp
- **备选方案**：Windows.Media.Playback（MediaPlayer）格式支持有限；FFmpeg 系无可靠 .NET 封装。
- **选定方案**：LibVLCSharp（VLC 的 .NET 绑定）。
  - 支持几乎所有音视频格式，硬件解码加速。
  - 可独立控制图片背景音乐的播放器实例，实现独立音量、淡入淡出。
  - 视频支持暂停、进度条拖拽、循环控制。
  - 部署时需附带原生 `libvlc` 文件夹，注意补齐依赖。

### 2.3 开发环境简化
- **无需 Visual Studio**：使用 .NET CLI（`dotnet`）可在 Trae 终端中完成项目创建、编译、运行和发布。  
- **AI 辅助编码**：DeepSeek V4 直接生成 XAML 界面代码和业务逻辑，省去可视化设计器依赖。  
- **工作流**：`提问 → AI 生成代码 → 人工审查调试 → 命令行构建/发布`，高效且无 IDE 绑定。

### 2.4 部署策略
- **自包含单文件发布**：一条命令生成单个 `.exe`，内含 .NET 运行时和应用代码。  
- **体积预估**：最终安装包约 200-300 MB（含 VLC 原生库），可直接通过 U 盘或网盘分发，运行免安装。

## 3. MVP 版本范围定义
MVP 旨在打通**中控操作 → 展示窗口全屏 → 媒体顺序切换**全链路，不包含现场应急和高级调节功能。

### 3.1 MVP 包含功能
- 播放列表的增删改、拖拽排序、清空。
- 独立展示窗口：可拖移、双击全屏、退出全屏恢复窗口。
- 图片显示：支持 JPG/PNG（含透明），图片无限期显示，手动切换。
- 视频显示：播放、暂停/继续、可拖拽进度条、循环开关（开启后无限循环）。
- 顺序控制：上一个/下一个、双击列表跳转。所有切换带淡入淡出转场（固定0.5秒）。
- 图片背景音乐：可添加单首音乐；音乐伴随图片出现淡入（3秒），切走时淡出（3秒）；音乐可设循环，循环期间若切走立刻停止。
- 持久化：关闭时自动保存当前播放列表及配置，启动时自动恢复。
- 基本错误处理：丢失文件时列表标红并跳过，播放时弹出轻微提示。

### 3.2 MVP 不包含功能
- 可配置转场类型/时长（写死淡入淡出0.5s）
- 黑屏/冻结画面
- 全局快捷键
- 播放过程中动态编辑列表（需先停止播放）
- 独立音量调节（使用系统主音量）
- 深色主题（仅亮色）
- 全局错误备用素材

## 4. 系统架构设计

### 4.1 分层架构
- **表示层 (UI)**  
  - `MainWindow`：中控面板（播放列表、控制按钮、当前状态）  
  - `ShowWindow`：独立的大屏展示窗口（无边框，可拖拽全屏）
- **业务逻辑层 (Service)**  
  - `PlaylistManager`：管理列表增删改排、持久化触发  
  - `PlaybackController`：响应播放指令，协调展示窗口切换  
  - `FileValidator`：文件存在性及格式校验
- **基础设施层 (Infrastructure)**  
  - `VlcMediaPlayer`：封装 LibVLCSharp，实现媒体播放暂停、音量、事件  
  - `PlaylistPersistence`：JSON 文件读写  
  - `Logger`：简单文本日志

### 4.2 设计模式
- **MVVM**：主界面使用数据绑定和命令，解耦视图与逻辑。  
- **独立展示窗口**：`ShowWindow` 为顶级窗口，由 `PlaybackController` 通过方法调用直接控制，内部完成媒体资源的加载和转场动画。

## 5. 模块分解

### 5.1 播放列表管理 (PlaylistManager)
- 持有 `ObservableCollection<PlaylistItem>`，对外暴露绑定源。
- 提供方法：`Add`、`Remove`、`Move`、`Clear`、`UpdateItem`。
- 任何修改都将通知持久化服务等待保存。

### 5.2 播放控制 (PlaybackController)
- 内部维护当前播放索引、列表引用。
- 核心方法：`PlayItem(PlaylistItem)`、`Next()`、`Previous()`、`JumpTo(index)`、`Stop()`。
- 在切换时：停止当前媒体 → 调起 `ShowWindow.TransitionTo(item)` → 执行淡入淡出。

### 5.3 展示窗口 (ShowWindow & MediaRenderer)
- `ShowWindow`：无边框、`AllowsTransparency="True"`、背景透明。通过 `DragMove()` 移动，双击或按钮切换全屏。内嵌两个层叠 `Canvas`（前层/后层），用于转场。
- `MediaRenderer`：管理 `VideoView`（LibVLC）和 `Image` 控件的创建与销毁，执行 `Opacity` 动画实现淡入淡出。
- 全屏逻辑：获取当前所在屏幕的工作区，设置窗口状态为最大化并去掉边框。

### 5.4 媒体适配层 (MediaAdapter)
- `VlcMediaPlayer` 封装：
  - 视频使用主播放器实例。
  - 背景音乐使用独立 `MediaPlayer`，但与视频播放器共享同一个 `LibVLC` 实例。
  - 淡入/淡出通过定时器平滑调整音频播放器的 `Volume` 属性实现（0 → 目标值 或 目标值 → 0）。
  - 提供事件：`MediaEnded`（视频结束）、`MusicPlaybackCompleted` 等。

### 5.5 工具与辅助
- `PlaylistPersistence`：使用 `System.Text.Json` 序列化播放列表到 `%AppData%\ShowPlayer\playlist.json`。  
- `FileValidator`：启动时校验所有文件，更新 `IsMissing` 属性。  
- `Logger`：记录操作日志到 `%AppData%\ShowPlayer\log.txt`，方便现场排查。

## 6. 核心数据结构

### 6.1 PlaylistItem
```csharp
public enum MediaType { Image, Video }

public class PlaylistItem : INotifyPropertyChanged
{
    public string Id { get; set; } = Guid.NewGuid().ToString();
    public MediaType Type { get; set; }
    public string FilePath { get; set; }
    public string? MusicPath { get; set; }
    public bool IsMusicLoop { get; set; }
    public bool IsVideoLoop { get; set; }
    public bool IsMissing { get; set; }
}
```
### 6.2 持久化示例
{
  "Items": [
    {
      "Id": "abc123",
      "Type": "Image",
      "FilePath": "D:\\assets\\logo.png",
      "MusicPath": "D:\\music\\award.mp3",
      "IsMusicLoop": true,
      "IsVideoLoop": false,
      "IsMissing": false
    }
  ]
}

## 7. 关键交互流程
### 7.1 启动与加载
App.xaml.cs 启动时读取 %AppData%\ShowPlayer\playlist.json。

若文件不存在或为空，则创建空列表。

初始化 MainWindow 并绑定 ViewModel，ViewModel 中注入 PlaylistManager（会触发文件校验，更新 IsMissing）。

创建并隐藏 ShowWindow，等待首次播放指令。

### 7.2 播放一个项目
用户双击列表项或点击“播放”。

PlaybackController.PlayItem(item)：

若当前有播放项，先停止并清理展示窗口。

根据 item.Type 调用 ShowWindow 准备媒体：

图片：设置 <Image Source="filePath"/>，若有有效 MusicPath 则启动背景音乐播放器（带3秒淡入）。

视频：将 VideoView 的媒体源设为文件路径，设置循环属性，开始播放。

执行转场动画：前层淡出、后层淡入（固定0.5秒）。

更新中控界面当前项高亮。

### 7.3 切换下一个
用户点击“下一个”按钮。

PlaybackController.Next()：

计算下一项索引（到末尾后停止，MVP不自动循环）。

调用 PlayItem(nextItem)，流程同7.2。

### 7.4 转场动画实现    
ShowWindow 内维护两个 ContentControl（或 Canvas）作为前后层。

切换时：

在后台层加载新媒体（透明度0）。
启动两个 DoubleAnimation：前台层 Opacity 1→0，后台层 Opacity 0→1，时长 500ms。
动画完成后，移除前台层的旧内容，将后台层转为前台层，实现交替。
### 7.5 图片背景音乐停止
当图片被切走或用户点击停止时，VlcMediaPlayer.StopMusic() 启动3秒音量递减（100→0），然后停止并释放资源。

### 7.6 视频循环与中断  
VlcMediaPlayer 中监听 MediaEnded 事件，若 IsLoop=true 则重新 Play。

用户点击切换时，直接调用 Stop()，立即结束循环并进入下一项。

### 7.7 持久化保存
主窗口 OnClosing 事件触发 PlaylistPersistence.Save(list)。

将 ObservableCollection<PlaylistItem> 序列化为 JSON，写入 %AppData%\ShowPlayer\playlist.json，若文件夹不存在则创建。
## 8. 项目结构
/ShowPlayer
  /src
    /ShowPlayer.App
      App.xaml / App.xaml.cs
      MainWindow.xaml / MainWindow.xaml.cs
      ShowWindow.xaml / ShowWindow.xaml.cs
      Models/
        PlaylistItem.cs
        Playlist.cs
      ViewModels/
        MainViewModel.cs
        PlaylistItemViewModel.cs
      Services/
        PlaylistManager.cs
        PlaybackController.cs
        PlaylistPersistence.cs
        FileValidator.cs
      Media/
        IMediaPlayer.cs
        VlcMediaPlayer.cs
        MediaRenderer.cs
      Helpers/
        Logger.cs

## 10. 构建与发布
### 10.1 日常构建
dotnet build -c Debug
### 10.2 MVP 发布命令 (绿色单文件)
bash
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o ./publish
发布后将 libvlc 文件夹（含大量 DLL 和 plugins 子目录）完整复制到 publish 目录，与 ShowPlayer.App.exe 并列。

### 10.3 最终交付物
压缩打包 publish 文件夹为 ShowPlayer_MVP.zip，解压即用，无需安装任何运行时。

## 11. MVP 开发建议顺序
骨架搭建：创建 WPF 项目，引入 VLC 包，实现 ShowWindow 可拖拽无边框窗口及全屏切换。

媒体播放验证：在展示窗口中用 LibVLC 播放视频和图片，实现基本暂停/继续/进度条。

播放列表 UI：主窗口的列表增删排序，绑定 ViewModel，支持数据持久化读写。

播放控制串联：实现 PlaybackController，完成“下一个”切换与转场动画。

背景音乐集成：为图片添加音乐播放器，淡入/淡出逻辑，循环开关。

错误处理：文件校验，丢失提示和跳过机制。

发布与现场测试：打包绿色版本，在真实多屏环境测试各项功能。

## 12. 风险与注意事项
VLC 原生库缺失：发布时一定要包含 libvlc 目录，否则运行报错。

屏幕索引：全屏到正确屏幕需用 Screen.FromHandle(window) 获取目标屏幕的工作区，并最大化到该区域。

淡入淡出实现：避免依赖 VLC 内部的不稳定淡出功能，采用音量属性平滑递减更可靠。

路径失效处理：素材文件移动后，播放列表中的路径将失效，需程序标记并提供重新定位的能力（完整版再完善）。

