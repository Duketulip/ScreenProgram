# 会场大屏播放控制软件 概要设计

## 1. 引言
本文档为该软件的总体技术架构设计，覆盖技术选型、系统模块划分、数据流和关键交互逻辑。  
开发环境：**Trae IDE**。部署目标：Windows 10/11 绿色免安装应用。

## 2. 技术选型

### 2.1 应用框架：WPF + .NET 8
- **WPF (.NET 8)**：原生 Windows 性能，多窗口控制成熟，XAML 布局灵活。
- **自包含发布**：`dotnet publish --self-contained` 生成单文件夹包，零运行时依赖。

### 2.2 媒体引擎：LibVLCSharp
- VLC 的 .NET 绑定，支持几乎所有音视频格式，硬件解码加速。
- 视频使用主播放器实例，背景音乐使用独立播放器实例，共享同一 `LibVLC` 实例。
- 部署时需附带 `libvlc` 原生库文件夹。

### 2.3 部署策略
- `dotnet publish --runtime win-x64 --self-contained true` 发布。
- 最终输出约 200-300 MB（含 .NET 运行时和 VLC 原生库）。
- 绿色免安装，解压即用。

## 3. 系统架构

### 3.1 分层架构

```
┌─────────────────────────────────────┐
│         表示层 (UI / XAML)           │
│  MainWindow          ShowWindow     │
│  (中控台)            (展示窗口)      │
├─────────────────────────────────────┤
│         ViewModel 层                │
│  MainViewModel                      │
│  (属性绑定、命令、事件处理)          │
├─────────────────────────────────────┤
│         业务逻辑层 (Services)        │
│  PlaylistManager    PlaybackController│
│  FileValidator      ThemeManager    │
├─────────────────────────────────────┤
│        基础设施层                    │
│  VlcMediaPlayer (LibVLCSharp封装)   │
│  PlaylistPersistence (JSON持久化)   │
│  Logger                             │
└─────────────────────────────────────┘
```

### 3.2 设计模式
- **MVVM**：MainWindow 使用数据绑定和命令模式，解耦视图与逻辑。
- **独立窗口控制**：ShowWindow 由 PlaybackController 通过方法调用直接控制，ViewModel 协调窗口状态。
- **事件驱动**：PlaybackController 抛出 ItemStarted、PlaybackStopped 等事件，ViewModel 和 App 响应。

## 4. 模块分解

### 4.1 App.xaml.cs（应用入口）
- 初始化持久化、设置、主题、媒体引擎。
- 创建 PlaylistManager、PlaybackController、ShowWindow、MainViewModel。
- 注册 ItemStarted 事件（展示窗口转场）。
- 设置 ShutdownMode = OnMainWindowClose，设置 MainWindow 属性。

### 4.2 MainWindow（中控台窗口）
- WPF 窗口，SizeToContent = Manual，固定初始尺寸 940x680。
- 数据上下文为 MainViewModel。
- 顶部工具栏：添加文件、清空列表、状态/文件名。
- 左侧列三区域：播放列表 → 播放控制 → 全局设置。
- 右侧列：选中项配置面板。
- 底部状态栏：时钟、列表项数、展示窗口切换按钮。
- 处理键盘快捷键、列表拖拽排序、视频进度条交互。

### 4.3 ShowWindow（展示窗口）
- 无边框独立窗口，黑色背景。
- 标题栏：播放文件名、置顶、全屏、最小化（两个最小化按钮）。
- 内容区：Image 控件（图片）和 VideoView（视频）叠加，切换可见性。
- 转场通过 FadeOverlay（半透明黑色 Border）的 Opacity 动画实现。
- 全屏：保存窗口位置尺寸，设置 WindowStyle.None，铺满屏幕。
- 退出全屏和关闭操作均转换为最小化，拦截 Closing 事件。
- Provide EnsureVisible() 恢复窗口显示。

### 4.4 MainViewModel（主视图模型）
- 实现 INotifyPropertyChanged。
- 维护两个核心属性：CurrentItem（列表选中项）、NowPlayingItem（当前播放项），完全分离。
- 公开属性：播放状态、进度、音量、转场设置、默认图片、选中项配置等。
- 公开 ICommand：添加、删除、清空、播放、上一个/下一个、停止、暂停/继续、选择音乐、选择图片等。
- 订阅 PlaybackController 事件。
- DispatcherTimer 驱动视频进度轮询（250ms 间隔）。
- 关闭时保存持久化。

### 4.5 PlaybackController（播放控制器）
- 内部维护 _playingItem 直接引用和 _currentIndex 并行索引。
- 核心方法：PlayItem(index/item)、Next()、Previous()、Stop()。
- Next() 到末尾后停止（不自动循环到开头），再从停止状态点 Next 回到索引 0。
- 事件：ItemStarted、PlaybackStopped、MediaEnded。

### 4.6 PlaylistManager（列表管理器）
- 封装 ObservableCollection<PlaylistItem>，对外暴露 ReadOnlyObservableCollection。
- 提供 Add、Remove、Move、Clear、UpdateItem。
- 任何修改自动触发 UI 更新（ObservableCollection 特性）。
- 使用 FileValidator 在添加/加载时校验文件存在性。

### 4.7 VlcMediaPlayer（媒体引擎封装）
- 封装 LibVLCSharp 的 MediaPlayer。
- 视频使用 _videoPlayer（主播放器），背景音乐使用 _musicPlayer（独立播放器）。
- 音乐淡入/淡出通过 System.Timers.Timer 逐步调整 Volume（30 步，每步 100ms，约 3 秒）。
- 事件：MediaEnded、MusicPlaybackCompleted。
- 视频循环通过 `--input-repeat=-1` 媒体选项实现。
- 音乐循环通过监听 EndReached 事件重新 Play。

### 4.8 PlaylistPersistence（持久化）
- 使用 System.Text.Json 序列化。
- 播放列表保存到 playlist.json，设置保存到 settings.json。
- 存放路径：%AppData%\ShowPlayer\。

### 4.9 Converters（值转换器）
- MissingBorderConverter：文件缺失 → 红色边框。
- MissingForegroundConverter：文件缺失 → 红色前景（当前未使用）。
- StringToVisibilityConverter：非空字符串 → Visible。
- BoolToVisibilityConverter：true → Visible。
- NullToVisibilityConverter：null → Visible（反向为 NotNullToVisibilityConverter）。
- InverseBoolToVisibilityConverter：false → Visible。
- InverseBoolConverter：布尔取反。

## 5. 核心数据结构

### 5.1 PlaylistItem
```csharp
public enum MediaType { Image, Video }

public class PlaylistItem : INotifyPropertyChanged
{
    string Id        // GUID
    MediaType Type   // Image 或 Video
    string FilePath  // 文件完整路径
    string? MusicPath  // 背景音乐路径（仅图片）
    bool IsMusicLoop   // 音乐循环
    bool IsVideoLoop   // 视频循环
    bool IsMissing     // 文件缺失标记
    bool IsNowPlaying  // 当前正在播放（驱动列表 ▶ 标记）
    string DisplayName => Path.GetFileName(FilePath)
}
```

### 5.2 AppSettings
```csharp
public class AppSettings : INotifyPropertyChanged
{
    TransitionType TransitionType     // Fade / Crossfade
    int TransitionDurationMs          // 100~5000ms
    string? FallbackImagePath         // 备选图片（未使用）
    string? FallbackVideoPath         // 备选视频（未使用）
    bool IsDarkTheme                  // 深色主题
    int VideoVolume                   // 0~100
    int MusicVolume                   // 0~100
    string? DefaultIdleImagePath      // 停止后默认展示图片
}
```

### 5.3 主要事件
```csharp
PlaybackController.ItemStarted    → (PlaylistItem item)
PlaybackController.PlaybackStopped → EventArgs
PlaybackController.MediaEnded     → EventArgs
VlcMediaPlayer.MediaEnded         → EventArgs
```

## 6. 关键交互流程

### 6.1 启动流程
```
App.OnStartup()
  → PlaylistPersistence.LoadSettings() → AppSettings
  → ThemeManager.Initialize(isDark)
  → PlaylistPersistence.Load() → List<PlaylistItem>
  → PlaylistManager.LoadFromPersistence()（含文件校验）
  → VlcMediaPlayer() → PlaybackController
  → ShowWindow()（SetMediaPlayer, SetTransitionConfig, Show）
  → MainViewModel(注入所有依赖)
  → 注册 ItemStarted 事件（ShowWindow 转场）
  → MainWindow(注入 ViewModel, Show)
```

### 6.2 播放流程
```
用户操作（双击列表/点播放/点从此项开始播放）
  → PlayItemCommand → PlaybackController.PlayItem(item/index)
  → StopCurrent()（停止当前媒体）
  → 更新 _currentIndex 和 _playingItem
  → ItemStarted 事件
    → App.xaml.cs: ShowWindow.TransitionTo(item)
      → 若转场为 Fade: FadeOverlay Opacity 动画 0→1→0
      → LoadContent(item)：切换 Image/VideoView 可见性并加载媒体
    → MainViewModel.OnItemStarted
      → 更新 NowPlayingItem、IsPlaying、StatusText
      → 若视频: 启动位置定时器
      → 若图片: 停止位置定时器，播放背景音乐
```

### 6.3 下一个/上一个
```
Next()
  → _currentIndex + 1
  → 若到达末尾: _currentIndex = Count, _playingItem = null, 停止, PlaybackStopped
  → 否则: PlayItem(nextIndex) → 同 6.2

Previous()
  → _currentIndex - 1
  → 若 < 0: 忽略
  → 否则: PlayItem(prevIndex) → 同 6.2
```

### 6.4 停止流程
```
StopCommand → PlaybackController.Stop()
  → StopCurrent()（StopVideo + StopMusicImmediate）
  → _currentIndex = -1, _playingItem = null
  → PlaybackStopped 事件
    → MainViewModel: 停止定时器、清进度、NowPlayingItem = null
    → ShowWindow.ShowDefaultImage(appSettings.DefaultIdleImagePath)
```

### 6.5 媒体结束自动跳转
```
VLC MediaEnded → PlaybackController.MediaEnded
  → MainViewModel.OnMediaEnded
    → Dispatcher.Invoke(() => PlaybackController.Next())
      → 若未勾选循环且还有下一项: PlayItem(nextIndex)
      → 若未勾选循环且已到最后: 停止 → 显示默认图片
      → 若勾选循环: VLC 自动重播（--input-repeat=-1）
```

### 6.6 关闭退出
```
MainWindow.OnClosing
  → _viewModel.SaveAll()
    → _positionTimer.Stop()
    → PlaylistPersistence.SaveSettings()
    → PlaylistManager.SaveToPersistence() → PlaylistPersistence.Save()
  → Application 退出（ShutdownMode.OnMainWindowClose）
  → 所有窗口关闭, VlcMediaPlayer 由 GC 清理
```

## 7. 项目结构
```
/ShowPlayer
├── start.ps1                  # 开发启动脚本
├── publish.ps1                # 发布打包脚本
├── 需求说明.md
├── 概要设计.md
├── 详细设计.md
└── src/ShowPlayer.App/
    ├── App.xaml / App.xaml.cs  # 应用入口
    ├── MainWindow.xaml / .cs   # 中控台窗口
    ├── ShowWindow.xaml / .cs   # 展示窗口
    ├── Models/
    │   ├── PlaylistItem.cs     # 播放列表项模型
    │   ├── AppSettings.cs      # 应用设置模型
    │   └── MediaType.cs        # 媒体类型枚举
    ├── ViewModels/
    │   ├── MainViewModel.cs    # 主视图模型
    │   ├── RelayCommand.cs     # 命令实现
    │   └── Converters.cs       # 值转换器
    ├── Services/
    │   ├── PlaylistManager.cs  # 列表管理
    │   ├── PlaybackController.cs # 播放控制
    │   ├── VlcMediaPlayer.cs   # 媒体引擎封装
    │   ├── PlaylistPersistence.cs # JSON 持久化
    │   ├── FileValidator.cs    # 文件校验
    │   ├── Logger.cs           # 日志
    │   └── ThemeManager.cs     # 主题管理
    ├── Views/
    │   ├── SettingsDialog.xaml / .cs
    │   └── EditItemDialog.xaml / .cs
    └── Themes/
        ├── DarkTheme.xaml
        └── LightTheme.xaml
```
