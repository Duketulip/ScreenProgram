# 修复记录

本文档记录根据 `问题建议.md`（第三方 AI 代码审查产出）确认并修复的所有问题。

## 高风险问题（6 个）

### P1: 缺失文件播放卡在当前索引

- **结论**: 真实 Bug ✅
- **问题描述**: `PlayItem()` 遇到 `item.IsMissing` 只记录日志并 `return`，不会播放下一项，也不会更新 `_currentIndex` 或触发停止。用户不断点"下一个"仍会反复命中同一缺失项。
- **修复方案**: 在 `PlayItem(index, attempt)` 递归调用中实现跳过逻辑，遇到缺失文件时自动尝试后续项，`attempt` 参数防无限递归。若缺失项之后没有有效项，则调用 `Stop()`，不绕回列表开头。
- **修改文件**: `Services/PlaybackController.cs`
  - 新增 `PlayItem(int, int)` 私有重载
  - 原 `PlayItem(int)` 改为委托给私有重载
  - 缺失时向后尝试 `index + 1`
  - 到达列表末尾仍无有效项时停止播放
- **验证方法**: 添加一个缺失文件到列表，播放该文件或点"下一个"经过该文件，应自动跳过并播放下一有效文件。

### P2: 图片背景音乐循环参数未生效

- **结论**: 真实 Bug ✅
- **问题描述**: `PlayMusic(filePath, loop)` 接收 `loop` 参数，但方法内部没有保存或使用该值；`OnMusicEndReached()` 只触发 `MusicPlaybackCompleted` 事件，没有重新播放的逻辑。
- **修复方案**: 在 `VlcMediaPlayer` 中新增 `_currentMusicPath`、`_currentMusicLoop` 和 `_musicGeneration` 字段。`PlayMusic()` 保存当前音乐状态。`OnMusicEndReached()` 中判断若 `_currentMusicLoop == true`，则在 VLC 事件回调返回后通过后台任务重启音乐，并用 generation 防止停止后旧循环请求复活。
- **修改文件**: `Services/VlcMediaPlayer.cs`
  - 新增字段 `_currentMusicPath`、`_currentMusicLoop`
  - `PlayMusic()` 保存参数到字段
  - `OnMusicEndReached()` 实现异步循环重播逻辑
  - 新增 `_musicGeneration` 防止过期重播请求生效
- **验证方法**: 为图片设置背景音乐并勾选"音乐循环"，音乐播完后应自动从头重播。

### P3: 音乐音量设置没有被新音乐和淡入目标使用

- **结论**: 真实 Bug ✅
- **问题描述**: `SetMusicVolume()` 只作用于当前存在的 `_musicPlayer`；新创建 `_musicPlayer` 时不会应用已保存的音量；`StartFadeIn()` 的目标音量硬编码为 `100`。用户设置的音乐音量在下次播放新音乐时失效。
- **修复方案**:
  - 新增 `_musicVolume` 字段（默认 80）
  - `SetMusicVolume()` 同时更新字段和当前播放器
  - `StartFadeIn()` 使用 `_musicVolume` 作为目标音量
  - `GetMusicVolume()` 返回 `_musicVolume` 字段
  - `App.OnStartup()` 调用 `SetMusicVolume(_appSettings.MusicVolume)` 应用持久化音量
  - 淡入/淡出使用 double 步进，避免低音量时整数步进为 0
- **修改文件**: `Services/VlcMediaPlayer.cs`
  - 新增字段 `_musicVolume`（默认 80）
  - `SetMusicVolume()`: `_musicVolume = value` + `_musicPlayer.Volume = _musicVolume`
  - `StartFadeIn()`: `var targetVolume = _musicVolume`
  - `App.xaml.cs`: 启动时设置音乐音量
- **验证方法**: 调整音乐音量滑块后，停止再播放新图片，检查背景音乐音量是否保持。

### P4: 转场期间快速切换会丢失展示窗口更新

- **结论**: 真实 Bug ✅
- **问题描述**: `TransitionTo()` 在 `_isTransitioning == true` 时直接 `return`。若操作员在淡入淡出过程中连续操作，中控状态已切换但展示窗口忽略后续请求，内容不一致。
- **修复方案**: 新增 `_pendingItem` 字段。转场中收到新请求时保存到 `_pendingItem`。当前转场动画完成（Fade 模式）或立即（Direct 模式）后，检查 `_pendingItem` 并自动执行下一个转场。
- **修改文件**: `ShowWindow.xaml.cs`
  - 新增字段 `_pendingItem`
  - `TransitionTo()`: 转场中时 `_pendingItem = item; return;`
  - Fade 动画完成回调末尾: 检查并处理 `_pendingItem`
  - Direct 分支末尾: 检查并处理 `_pendingItem`
- **验证方法**: 在列表中快速双击不同项目，展示窗口应逐个切换到最终选择的项目，不会卡在中间状态。

### P5: 图片音乐开始时机早于画面切入

- **结论**: 真实 Bug ✅
- **问题描述**: `OnItemStarted()` 立即调用 `PlayMusicForCurrent()`，但 Fade 模式下图片要等黑场渐入完成后才 `LoadContent()`。用户先听到音乐后看到画面。
- **修复方案**: 
  - `ShowWindow` 新增 `ContentLoaded` 事件
  - `TransitionTo()` 中加载内容完成后触发 `ContentLoaded`
  - `MainViewModel` 订阅 `ContentLoaded` 事件，仅在图片内容实际加载完成后调用 `PlayMusicForCurrent()`
  - `OnItemStarted()` 不再直接播放音乐
- **修改文件**: 
  - `ShowWindow.xaml.cs`: 新增 `ContentLoaded` 事件，`LoadContent()` 后触发
  - `MainViewModel.cs`: 订阅 `ContentLoaded`，`OnShowContentLoaded()` 中播放音乐；`OnItemStarted()` 中移除音乐启动代码
- **验证方法**: 切换到一张带背景音乐的图片，应看到画面后再听到音乐淡入，而非先闻声后见图。

### P6: 全屏总是铺主屏

- **结论**: 真实 Bug ✅
- **问题描述**: `EnterFullscreen()` 使用 `SystemParameters.PrimaryScreenWidth/Height` 并把 `Left/Top` 固定为 `0,0`。展示窗口拖到扩展屏后双击全屏，仍会回到主屏。
- **修复方案**: 使用 Win32 API `MonitorFromWindow` + `GetMonitorInfo` 获取展示窗口当前所在显示器的真实边界，用该边界设置全屏位置和尺寸。新增 `NativeMethods` 内部类和 `GetCurrentScreenBounds()` 辅助方法。
- **修改文件**: 
  - `ShowWindow.xaml.cs`
    - 新增 `NativeMethods` 内部静态类（`MonitorFromWindow`、`GetMonitorInfo`、`MONITORINFO`、`RECT`）
    - 新增 `GetCurrentScreenBounds()` 方法
    - `EnterFullscreen()` 使用 `GetCurrentScreenBounds(this)` 获取屏幕边界
- **验证方法**: 将展示窗口拖到扩展屏后双击全屏，应铺满扩展屏而非主屏。

## 中风险问题（4 个）

### M1: 快捷键未设置 `e.Handled`

- **结论**: 真实 Bug ✅
- **问题描述**: `PreviewKeyDown` 调用快捷键逻辑后没有标记事件已处理，空格键可能同时触发焦点控件的默认行为。
- **修复方案**: `HandleKeyDown()` 返回 `bool` 表示是否处理；`MainWindow` 中的事件处理根据返回值设置 `e.Handled = true`。
- **修改文件**: 
  - `ViewModels/MainViewModel.cs`: `HandleKeyDown()` 返回值改为 `bool`，处理后 `return true`
  - `MainWindow.xaml.cs`: `OnPreviewKeyDown` 中 `if (HandleKeyDown(...)) e.Handled = true`
- **验证方法**: 焦点在复选框或按钮上时按空格，应只触发"下一个"而非同时激活焦点控件。

### M2: 展示窗口 `ShowInTaskbar` 与需求矛盾

- **结论**: 真实 Bug ✅
- **问题描述**: 需求要求展示窗口最小化到任务栏，但 XAML 设为 `ShowInTaskbar="False"`，退出全屏后也未恢复。
- **修复方案**: XAML 默认 `ShowInTaskbar="True"`；`ExitFullscreen()` 中恢复 `ShowInTaskbar = true`；全屏时保持 `false`。
- **修改文件**: 
  - `ShowWindow.xaml`: `ShowInTaskbar="True"`
  - `ShowWindow.xaml.cs`: `ExitFullscreen()` 中 `ShowInTaskbar = true`
- **验证方法**: 最小化展示窗口后应能在任务栏看到并点击恢复。

### M3: 拖拽排序可能拖动之前选中的项目

- **结论**: 真实 Bug ✅
- **问题描述**: 拖拽使用 `listBox.SelectedItem`，而 `PreviewMouseLeftButtonDown` 发生在 ListBox 更新选择之前。从非选中行拖拽时拿到旧选中项。
- **修复方案**: 在 `PreviewMouseLeftButtonDown` 中从 `OriginalSource` 向上查找 `ListBoxItem`，以该容器的 `DataContext` 为选中项并同步到 `SelectedItem`。
- **修改文件**: `MainWindow.xaml.cs`
  - `OnListBoxPreviewMouseLeftButtonDown`: 用 `FindAncestor<ListBoxItem>()` 找到实际行
  - 新增 `FindAncestor<T>()` 泛型辅助方法
- **验证方法**: 选中第 3 项后，从第 1 项开始拖拽，应移动第 1 项而非第 3 项。

### M4: 退出时没有显式释放 VLC 资源

- **结论**: 真实 Bug ✅
- **问题描述**: `VlcMediaPlayer` 实现了 `IDisposable`，但 `App.OnExit()` 未调用 `Dispose()`。VLC 原生资源、文件句柄和后台事件未明确释放。
- **修复方案**: `App.OnExit()` 中停止播放、调用 `_mediaPlayer.Dispose()`、关闭展示窗口。完善 `VlcMediaPlayer.Dispose()` 确保取消事件订阅、停止并释放所有播放器和 `LibVLC` 实例。
- **修改文件**: 
  - `App.xaml.cs`: 新增 `OnExit()`，调用 `_playbackController.Stop()` + `_mediaPlayer.Dispose()` + `_showWindow.Close()`
  - `Services/VlcMediaPlayer.cs`: `Dispose()` 中取消事件订阅、停止并释放 `_videoPlayer`、`_musicPlayer`、`_libVlc`
- **验证方法**: 关闭中控台后检查进程管理器中无残留的 `ShowPlayer.App` 进程。

## 交互与代码优化建议（4 个）

### #13: 默认图片路径缺失时没有 UI 提示

- **结论**: 真实问题 ✅
- **问题描述**: `ShowDefaultImage()` 的 `catch` 块只清空 `Source`，无日志无状态提示。用户设置默认图片后如果路径失效，现场表现是黑屏，排查成本高。
- **修复方案**: 空路径/文件不存在时写 Info/Warning 日志；加载成功写 Info；异常时写 Error 日志并附带异常信息。
- **修改文件**: `ShowWindow.xaml.cs`
  - `ShowDefaultImage()`: 增加 `Logger.Info/Warning/Error` 调用
- **验证方法**: 设置一个不存在的默认图片路径后停止播放，查看日志文件应有 Warning 记录。

### #14: `TransitionType.Crossfade` 重命名为 `Direct`

- **结论**: 真实问题 ✅
- **问题描述**: `TransitionType.Crossfade` 实际行为是"直接切换"（无过渡效果），不是真正交叉淡化。枚举名与行为不符，容易误导维护者。
- **修复方案**: 枚举值 `Crossfade` → `Direct`。ViewModel 属性和 XAML 绑定同步更名。所有引用处更新。遗留的 `SettingsDialog.xaml.cs` 一并修复。
- **修改文件**: 
  - `Models/AppSettings.cs`: `Crossfade` → `Direct`
  - `ViewModels/MainViewModel.cs`: `UseCrossfade` → `UseDirectTransition`，逻辑使用 `TransitionType.Direct`
  - `MainWindow.xaml`: 绑定属性名更新
  - `ShowWindow.xaml.cs`: 条件判断 `TransitionType.Direct`
  - `Views/SettingsDialog.xaml.cs`: `TransitionType.Crossfade` → `TransitionType.Direct`
- **验证方法**: 编译通过，全局设置中"直接"选项的行为不变（无过渡切换）。

### #15: 发布/开发脚本会强制结束所有 dotnet 进程

- **结论**: 真实问题 ✅
- **问题描述**: `Get-Process -Name "ShowPlayer.App","MSBuild","VBCSCompiler","dotnet" | Stop-Process -Force` 会杀掉机器上其他项目的 `dotnet`、MSBuild 或编译服务。
- **修复方案**: 只杀掉 `ShowPlayer.App` 进程，移除对其他进程的强制终止。
- **修改文件**: 
  - `start.ps1`: `Get-Process -Name "ShowPlayer.App"` 仅限本应用
  - `publish.ps1`: 同上
- **验证方法**: 执行脚本时不影响其他项目的 dotnet/MSBuild 编译过程。

### #16: 仓库里没有忽略构建产物

- **结论**: 真实问题 ✅
- **问题描述**: `.gitignore` 只有 `/debug.log` 和 `/ShowPlayer_v1.0.0.zip` 两行，`bin/`、`obj/`、发布产物、VLC 插件文件等未被忽略。
- **修复方案**: 补充 `**/bin/`、`**/obj/`、`*.user`、`.vs/`、`packages/`、publish 目录、`libvlc/` 等通配规则。
- **修改文件**: `.gitignore`
- **验证方法**: `git status` 应不再显示 `bin/` 和 `obj/` 下的文件。

---

## 未处理问题说明

| # | 问题 | 理由 |
|---|------|------|
| #7 | 播放/暂停按钮文字与实际状态不同步 | 边缘情况，仅在高频快速切换时出现。实际使用中影响极小 |
| #11 | 音乐文件缺失只记日志 | 合理改进但非紧迫，需增加模型属性 `IsMusicMissing` |
| #17 | 建议增加测试 | 合理建议但无现成测试框架，优先级低 |

## 修复统计

| 类别 | 总数 | 已修复 | 未处理 |
|------|------|--------|--------|
| 高风险 Bug | 6 | 6 | 0 |
| 中风险 Bug | 6 | 4 | 2 (#7、#11) |
| 优化建议 | 5 | 4 | 1 (#17) |
| **合计** | **17** | **14** | **3** |

---

## 二次复核修复记录（本轮）

第三方复核后发现，部分“已修复”问题仍存在边界行为或引入了新风险。本轮已继续修复如下：

### R1: 缺失文件跳过不再绕回列表开头

- **关联原问题**: P1
- **复核发现**: 原修复使用 `(index + 1) % Count`，如果缺失项位于列表末尾，会绕回开头播放，破坏“播放到末尾后停止”的既有语义。
- **修复方案**: `PlaybackController.PlayItem(index, attempt)` 遇到缺失项时只向后查找；若已经到列表末尾，则调用 `Stop()`，不再取模回到 0。
- **修改文件**: `src/ShowPlayer.App/Services/PlaybackController.cs`

### R2: 启动时应用已保存的音乐音量

- **关联原问题**: P3
- **复核发现**: `VlcMediaPlayer` 内部保存了 `_musicVolume`，但启动时只调用 `SetVideoVolume()`，没有把 `settings.json` 中的 `MusicVolume` 写入播放器，第一次播放音乐仍可能使用默认 80。
- **修复方案**: `App.OnStartup()` 创建 `VlcMediaPlayer` 后同时调用 `_mediaPlayer.SetMusicVolume(_appSettings.MusicVolume)`。
- **修改文件**: `src/ShowPlayer.App/App.xaml.cs`

### R3: 音乐循环避免在 VLC EndReached 回调中直接 Stop/Dispose/Play

- **关联原问题**: P2
- **复核发现**: 原修复在 `OnMusicEndReached()` 中直接调用 `StopMusicImmediate()` 和 `PlayMusic()`，这些操作包含 Stop/Dispose/创建播放器，放在 VLC 回调线程内有重入、死锁或崩溃风险。
- **修复方案**: `OnMusicEndReached()` 只捕获当前音乐路径、循环标记和 generation；使用后台任务延迟极短时间后再重启音乐。新增 `_musicGeneration`，停止或切换音乐时使旧的循环重启请求失效。
- **额外修复**: 淡入/淡出步进改为 double，避免目标音量低于 30 时整数步进为 0 导致 Timer 永不结束。
- **修改文件**: `src/ShowPlayer.App/Services/VlcMediaPlayer.cs`

### R4: 停止/默认图加载会取消未完成转场

- **关联原问题**: P4、P5
- **复核发现**: Fade 动画回调没有取消机制。转场中点击停止或显示默认图后，旧动画 `Completed` 仍可能执行 `LoadContent(item)`，导致停止后旧素材重新出现在展示窗口。
- **修复方案**: `ShowWindow` 新增 `_transitionVersion` 和 `CancelTransition()`；`Stop()`、`ShowDefaultImage()` 会取消 `FadeOverlay` 动画、清空 `_pendingItem`、递增版本号，使旧动画回调自动失效。
- **修改文件**: `src/ShowPlayer.App/ShowWindow.xaml.cs`

### R5: ContentLoaded 只允许当前播放项启动音乐

- **关联原问题**: P5
- **复核发现**: `OnShowContentLoaded(item)` 没有校验传入 item 是否仍是 `NowPlayingItem`。快速切换时，旧画面完成加载可能触发当前项音乐，造成画面和音乐错配。
- **修复方案**: 仅当 `item == NowPlayingItem && item.Type == MediaType.Image` 时调用 `PlayMusicForCurrent()`。
- **修改文件**: `src/ShowPlayer.App/ViewModels/MainViewModel.cs`

### R6: 旧设置文件中的 Crossfade 兼容迁移

- **关联原问题**: #14
- **复核发现**: 枚举从 `Crossfade` 改为 `Direct` 后，旧 `settings.json` 中若保存 `"TransitionType": "Crossfade"`，反序列化会失败并回退默认设置。
- **修复方案**: `LoadSettings()` 读取 JSON 后先将 `"Crossfade"` 替换为 `"Direct"` 再反序列化。设置对话框文案同步改为 `Direct`。
- **修改文件**:
  - `src/ShowPlayer.App/Services/PlaylistPersistence.cs`
  - `src/ShowPlayer.App/Views/SettingsDialog.xaml`

### R7: 展示窗口退出时绕过关闭保护

- **关联原问题**: M4
- **复核发现**: `App.OnExit()` 调用 `_showWindow.Close()` 仍可能被 `ShowWindow.OnClosing()` 的最小化保护拦截。
- **修复方案**: `ShowWindow` 新增 `ForceClose()` 和 `_forceClose` 标记；应用退出时调用 `_showWindow.ForceClose()`。
- **修改文件**:
  - `src/ShowPlayer.App/App.xaml.cs`
  - `src/ShowPlayer.App/ShowWindow.xaml.cs`

### R8: 快捷键左右键仅在视频播放时拦截

- **关联原问题**: M1
- **复核发现**: 原 `HandleKeyDown()` 对左右键总是返回 true，即使当前不是视频，也会吞掉控件自身键盘行为。
- **修复方案**: 左右键只有在 `IsNowPlayingVideo` 时执行 seek 并返回 true；其他场景返回 false 放行。
- **修改文件**: `src/ShowPlayer.App/ViewModels/MainViewModel.cs`

### R9: 修复 start.ps1 清理后 WPF 生成文件缺失导致编译失败

- **触发现象**: 运行 `.\Start.ps1` 后，脚本删除 `obj/bin`，随后在项目目录执行 `dotnet build --nologo`，可能出现 `CS2001: 未能找到源文件 ... obj\Debug\net8.0-windows\*.g.cs`。
- **原因判断**: 该问题不是业务代码错误，而是 WPF 临时项目在项目目录直接构建时对已删除 `obj` 生成文件的引用失效。从解决方案根目录构建 `ScreenProgram.sln` 可以稳定重新生成 WPF `*.g.cs` 文件。
- **修复方案**:
  - `start.ps1` 不再 `Push-Location` 到项目目录直接 `dotnet build`。
  - 改为在 `$PSScriptRoot` 下执行 `dotnet build $SolutionPath --nologo`。
  - 清理路径改为显式清理 `$ProjectDir\obj` 和 `$ProjectDir\bin`。
  - `publish.ps1` 同步改为从仓库根目录执行 `dotnet restore $ProjectPath` 和 `dotnet publish $ProjectPath`，避免发布脚本触发同类问题。
- **修改文件**:
  - `start.ps1`
  - `publish.ps1`
- **验证方法**:
  - 清理 `src\ShowPlayer.App\obj` 和 `src\ShowPlayer.App\bin` 后执行 `dotnet build ScreenProgram.sln --nologo`，通过。
  - 清理后执行根目录 project-path 形式的 `dotnet publish`，通过。

### 二次验证

- `dotnet build --nologo` 已通过。
- 构建结果：0 warning，0 error。
- 已额外验证清理 `bin/obj` 后的 solution 构建和 project-path 发布命令均可通过。
