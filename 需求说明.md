# 会场大屏播放控制软件 需求规格说明书

## 1. 概述
本软件运行于 Windows 系统，面向颁奖、演出等现场活动。  
主显示器作为中控台，**展示内容通过一个独立的可拖拽窗口输出**，该窗口可移动至任意屏幕并一键全屏，完美适配不同会场的屏幕布局。  
操作人员通过中控界面编排播放列表，实时控制大屏显示的内容。软件支持图片（可选配背景音乐）与视频的混合播放、顺序切换、直接跳转，并允许在播放过程中临时编辑列表。

## 2. 系统运行环境
- **操作系统**：Windows 10/11（64位）  
- **屏幕支持**：  
  - 至少需要一台显示器（主屏）作为中控操作区。  
  - 展示内容以一个**独立的可拖拽窗口**呈现。  
  - 该窗口可被拖放至任意物理屏幕（包括扩展屏或复制屏），并在目标屏幕上触发**全屏化**，也可保留窗口模式方便调试。  
- **音频**：跟随系统默认音频输出设备，软件内不提供设备选择功能。

## 3. 功能需求

### 3.1 播放列表管理
1. **列表编辑**  
   - 支持添加素材：图片（JPG、PNG，含透明通道）和视频（MP4、MOV 等常见格式）。  
   - 每张图片可单独配置背景音乐（MP3、WAV等），并独立控制音乐是否循环。  
   - 每个视频可独立设置是否循环播放（默认不循环）。  
   - 提供删除单项、清空列表、拖拽排序或上下按钮移动顺序。  
   - 支持修改某一项的配置：替换文件、更换背景音乐、开关循环。  
   - **列表不保存为独立文件**，但关闭软件时会自动记忆当前列表及所有配置，下次启动时自动还原。

2. **播放中编辑**  
   - 播放期间仍然允许添加、删除、排序或修改配置，改动立即生效。  
   - 若正在播放的项被删除，则立即停止该内容的呈现，并根据规则显示下一项或错误备选内容。

### 3.2 图片展示与背景音乐
- **显示规则**：图片无限期显示，直到操作者手动执行“下一个”或跳转指令，无自动倒计时。  
- **背景音乐行为**：  
  - 图片出现时，关联音乐立即播放，带 **3 秒淡入** 效果。  
  - 图片被切走时，音乐立即停止，并带 **3 秒淡出** 效果。  
  - 若音乐时长 < 图片显示时长，音乐自动循环播放，衔接处自然过渡。  
  - 音乐音量独立调节，不影响视频音量，调节实时生效。

### 3.3 视频展示控制
- **播放行为**：  
  - 开启循环：播毕后自动从头开始，不间断循环，直至人为切走。  
  - 未开启循环：播放完毕后定格在最后一帧。  
- **切换规则**：  
  - 循环或非循环状态下，点击“下一个”均**立即中断当前视频**并切换。  
- **控制功能**：  
  - 暂停/继续：视频冻结并静音，或恢复播放。  
  - 进度条：可拖拽跳转至任意时间点（跨度可任意，无论是否循环）。

### 3.4 转场效果
- 所有素材切换均应用转场，提供以下两种模式：  
  - **淡入淡出**（交叉过渡，默认）  
  - **渐变**（如黑场渐隐再渐显）  
- 转场时长可配置（默认 0.5 秒）。  
- 效果作用于图片→图片、图片→视频、视频→图片、视频→视频所有组合。

### 3.5 展示窗口特性（独立窗口 + 拖放 + 全屏）
- 展示内容运行在一个**独立于中控的窗口**中。  
- 该窗口默认以**无边框窗口**形式启动，可被拖拽至任意屏幕。  
- 提供“**全屏**”按钮或双击窗口触发目标屏幕全屏（覆盖整个目标显示器）。  
- 从全屏退出后窗口恢复原尺寸和位置，方便再次调整。  
- 在单屏调试时，可将该窗口与中控界面摆放在同一屏幕，以窗口方式运行，便于开发测试。

### 3.6 中控界面功能
- **信息显示**  
  - 当前播放项缩略图预览（实时同步展示窗口画面）。  
  - 下一项缩略图及名称。  
  - 当前项序号、类型、循环状态。  
- **控制按钮**  
  - 播放、暂停（仅视频有效）、停止（恢复至黑屏待机）。  
  - 上一个/下一个（顺序切换，均履行转场效果）。  
  - 双击列表项直接跳转播放。  
  - **黑屏**：展示窗口显示纯黑，再次点击恢复当前内容。  
  - **冻结**：锁定展示窗口当前帧，后台可继续播放或静止，解除后恢复实时。  
- **快捷键**（中控窗口聚焦时生效）  
  - `空格`：下一个  
  - `B`：黑屏/恢复  
  - `F`：冻结/解除  
  - `Esc`：紧急黑屏并静音，再按恢复  
  - `←/→`：视频快退/快进（步长可设）  
  - `Ctrl+P`：暂停/继续视频  
- **状态栏**  
  - 系统时间、总项数、当前播放进度时间码（视频）。  
  - 音乐独立音量条、视频音量条。

### 3.7 错误处理与备选素材
- 若某素材文件丢失或无法解码，列表中以异常标识高亮提示。  
- 播放到异常项时：  
  - 如果设置了**全局默认错误备选**（可分别指定一张图片和一个视频），则自动展示该备选内容。  
  - 如果没有备选，则展示窗口冻结上一帧，中控醒目报错，等待操作员手动干预。  
- 所有错误写入日志，方便事后排查。

## 4. 用户界面总体要求
- 中控面板适应触控与鼠标操作，按钮尺寸适中、布局清晰。  
- 播放列表以列表形式呈现，每项含缩略图、文件名、类型图标、循环标记、音乐标记。  
- 支持深色/亮色主题，至少提供深色模式以适应暗光环境。  
- 展示窗口无菜单栏、无标题栏（全屏时），窗口模式下保留最简控制条（含全屏按钮和退出全屏按钮）。

## 5. 性能与稳定性
- 图片与音乐切换时，淡入淡出流畅，无爆音或画面卡顿。  
- 视频解码启用硬件加速，支持 1080p 及以上常见码率。  
- 可连续稳定运行数小时，无内存泄漏。  
- 当展示窗口所在屏幕因连接断开或其他原因消失时，软件应能检测到并在该屏幕恢复连接后，自动重新全屏并继续播放（视频从断开时的时间点恢复，图片/音乐按规则恢复）。

## 6. 限制与约束
- 仅允许本机直接操作，不提供网络远程控制。  
- 音频设备不可选，跟随系统默认。  
- 不支持多播放列表并发、画中画等高级功能。  
- 转场效果固定为淡入淡出和渐变，暂不开放自定义。  
- 素材必须为本地文件，不提供在线流媒体播放。