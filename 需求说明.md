# 会场大屏播放控制软件 需求规格说明书

## 1. 概述
本软件运行于 Windows 系统，面向颁奖、演出等现场活动。  
主显示器作为中控台，**展示内容通过一个独立的可拖拽窗口输出**，该窗口可移动至任意屏幕并一键全屏，完美适配不同会场的屏幕布局。  
操作人员通过中控界面编排播放列表，实时控制大屏显示的内容。软件支持图片（可选配背景音乐）与视频的混合播放、顺序切换、直接跳转。

## 2. 系统运行环境
- **操作系统**：Windows 10/11（64位）  
- **运行时**：无需安装 .NET 运行时（自包含发布）  
- **屏幕支持**：  
  - 中控台窗口为主显示器操作区。  
  - 展示窗口独立于中控，可拖放至任意物理屏幕并全屏化，也可保留窗口模式。  
- **音频**：跟随系统默认音频输出设备。

## 3. 功能需求

### 3.1 播放列表管理
1. **列表编辑**  
   - 支持添加素材：图片（JPG、PNG）和视频（MP4、MOV、AVI、MKV、WMV）。  
   - 每张图片可单独配置背景音乐（MP3、WAV、WMA、AAC、FLAC），并独立控制音乐是否循环。  
   - 每个视频可独立设置是否循环播放（默认不循环）。  
   - 提供删除单项（列表中每行末尾 ✕ 按钮）、清空列表、拖拽排序。  
   - 关闭软件时自动保存当前列表及配置，下次启动时自动还原。

2. **播放中编辑**  
   - 播放期间允许添加、删除、排序，改动立即生效。  
   - 若正在播放的项被删除，则立即停止播放，展示默认图片（若已设置）或黑屏。

### 3.2 选中项（配置面板）
中控右侧为选中项配置区域，在列表中单击选中一项后：
- 显示选中文件名。
- "从此项开始播放"按钮（仅当选中项不是正在播放项时显示）。
- 视频循环播放开关（仅对视频类型）。
- 背景音乐设置（仅对图片类型）：选择音乐文件、清除音乐、音乐循环开关。

### 3.3 图片展示与背景音乐
- **显示规则**：图片无限期显示，直到操作者手动切换。
- **背景音乐行为**：  
  - 图片出现时，关联音乐立即播放，带 **3 秒淡入**。  
  - 图片被切走时，音乐立即停止（带 **3 秒淡出**）。
  - 音乐音量独立调节，不影响视频音量。
- 图片在展示窗口中以 `Uniform` 缩放显示。

### 3.4 视频展示控制
- **播放行为**：  
  - 开启循环：播毕后自动从头开始，不间断循环。  
  - 未开启循环：播毕后自动跳转到列表下一项（通过事件驱动）。
- **切换规则**：点击"上一个/下一个"或跳转时，**立即中断当前视频**并切换。
- **控制功能**：  
  - 暂停/继续：按钮文字自动切换（播放/暂停）。  
  - 进度条：可拖拽跳转至任意时间点（仅视频播放时显示进度条和时间码）。

### 3.5 转场效果
- 所有素材切换均应用转场，提供两种模式：  
  - **淡入淡出**（黑场过渡，默认）。  
  - **直接切换**（无过渡效果）。  
- 转场时长可配置（100ms ~ 3000ms，默认 500ms）。
- 效果作用于图片、视频及其混合切换。

### 3.6 展示窗口特性
- 展示内容运行在**独立无边框窗口**中，可拖拽至任意屏幕。
- 双击内容区触发目标屏幕全屏；全屏时鼠标移到顶部显示控制栏。
- 按 **Esc** 退出全屏；全屏提示 3 秒后自动消失。
- 标题栏提供：置顶、全屏、最小化按钮（无关闭按钮，仅可最小化）。
- 所有最小化/关闭操作均为最小化到任务栏，通过中控台"显示展示窗口"恢复。
- 窗口可通过中控台右下角"隐藏/显示展示窗口"按钮控制。
- 标题栏在窗口模式下显示当前播放文件名。

### 3.7 默认闲置图片
- 全局设置中可指定一张默认图片。
- 停止播放（点击停止按钮/播到末尾）后，自动在展示窗口显示该图片。
- 若未设置默认图片，停止后展示黑屏。

### 3.8 中控界面布局

#### 顶部工具栏
| 左侧 | 中间（切换显示） | 右侧 |
|------|----------------|------|
| 添加文件、清空列表 | 未播放：状态文字 / 播放中：当前文件名 | 无 |

#### 左侧列（上到下）
1. **播放列表** — 每项含播放标记 ▶（当前播放）、类型图标、文件名、删除按钮 ✕，支持拖拽排序。
2. **播放控制**（仅在有播放内容时显示）— 文件名方块、上一个/播放暂停/停止/下一个按钮、视频进度条（仅视频时显示）。
3. **全局设置** — 转场类型选择、默认图片选择、淡入时长滑条、视频音量、音乐音量。

#### 右侧列
**选中项** — 配置选中项的循环和音乐设置。

#### 状态栏
系统时钟、列表总项数、显示/隐藏展示窗口按钮。

### 3.9 快捷键
中控窗口聚焦时生效：
- `空格`：下一个
- `Ctrl+P`：暂停/继续视频
- `←/→`：视频快退/快进 5 秒

### 3.10 错误处理
- 文件丢失时列表中以红色字体和红色边框高亮。
- 播放到缺失文件时跳过（仅日志记录报错）。
- 所有操作写日志到 `%AppData%\ShowPlayer\log.txt`。

### 3.11 主题
- 支持深色/亮色主题，启动时根据设置加载。
- 当前在中控界面全局设置中未暴露主题切换按钮，保留在 `AppSettings` 中。

### 3.12 持久化
- **播放列表**：`%AppData%\ShowPlayer\playlist.json`
- **设置**：`%AppData%\ShowPlayer\settings.json`
- 应用退出时自动保存。
- 下次启动自动还原列表和设置。

## 4. 用户界面总体要求
- 中控面板适应鼠标操作，按钮尺寸适中、布局清晰。
- 展示窗口无菜单栏、无标题栏（全屏时），窗口模式下保留最简控制条。
- 支持深色/亮色主题。

## 5. 性能与稳定性
- 图片与音乐切换时，淡入淡出流畅。
- 视频解码启用硬件加速。
- 可连续稳定运行数小时。
- 自包含发布，总大小约 200-300 MB（含 VLC 原生库）。

## 6. 限制与约束
- 仅限本机操作，不提供网络远程控制。
- 音频设备跟随系统默认。
- 素材必须为本地文件。
