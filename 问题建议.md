# 会场大屏播放控制软件 问题建议

本文基于 `需求说明.md`、`概要设计.md`、`详细设计.md` 以及当前 `src/ShowPlayer.App` 源码审查整理，供原项目开发者确认后修复潜在 bug、优化交互设计和代码逻辑。

## 高风险问题

### 1. 缺失文件播放会卡在当前索引，未真正“跳过”

- 位置：`src/ShowPlayer.App/Services/PlaybackController.cs:32`、`src/ShowPlayer.App/Services/PlaybackController.cs:41`、`src/ShowPlayer.App/Services/PlaybackController.cs:61`
- 现象：`PlayItem()` 遇到 `item.IsMissing` 只记录日志并 `return`，不会播放下一项，也不会更新 `_currentIndex` 或触发停止。
- 风险：当视频自然结束后 `Next()` 命中缺失文件时，控制器可能一直停在同一个 nextIndex；继续点“下一个”仍会反复命中同一缺失项，和需求“播放到缺失文件时跳过”不一致。
- 建议：播放前重新校验 `File.Exists(item.FilePath)`；缺失时标记 `IsMissing = true` 并继续寻找下一项。注意避免全列表缺失时无限递归，可使用循环和最多尝试次数。

### 2. 图片背景音乐循环参数未生效

- 位置：`src/ShowPlayer.App/Services/VlcMediaPlayer.cs:102`、`src/ShowPlayer.App/Services/VlcMediaPlayer.cs:251`
- 现象：`PlayMusic(string filePath, bool loop, ...)` 接收 `loop` 参数，但方法内部没有保存或使用该值；`OnMusicEndReached()` 只触发 `MusicPlaybackCompleted`，没有重新播放，且当前没有订阅者。
- 风险：图片项勾选“音乐循环”后实际不会循环，直接违反需求。
- 建议：为音乐播放器保存 `_isMusicLoop` 和 `_currentMusicPath`，在 `EndReached` 中根据 loop 重新创建/播放媒体；或给音乐 `Media` 添加可验证的 VLC 循环选项。事件来自 VLC 后台线程，重播逻辑要做好状态锁和退出时保护。

### 3. 音乐音量设置没有被新音乐和淡入目标使用

- 位置：`src/ShowPlayer.App/ViewModels/MainViewModel.cs:166`、`src/ShowPlayer.App/Services/VlcMediaPlayer.cs:82`、`src/ShowPlayer.App/Services/VlcMediaPlayer.cs:150`、`src/ShowPlayer.App/Services/VlcMediaPlayer.cs:158`
- 现象：`SetMusicVolume()` 只作用于当前存在的 `_musicPlayer`；启动时也没有像视频一样设置音乐音量。新播放音乐时 `StartFadeIn()` 的目标音量硬编码为 `100`。
- 风险：用户保存的音乐音量在下次启动、下一首音乐、淡入结束后都可能失效；音乐音量滑条体验不可信。
- 建议：在 `VlcMediaPlayer` 内维护 `_musicVolume` 字段，`SetMusicVolume()` 同时更新字段和当前播放器；`StartFadeIn()` 使用该字段作为目标音量。`App.OnStartup()` 或 `MainViewModel` 构造时应初始化音乐音量。

### 4. 转场期间快速切换会丢失展示窗口更新

- 位置：`src/ShowPlayer.App/ShowWindow.xaml.cs:47`、`src/ShowPlayer.App/ShowWindow.xaml.cs:50`、`src/ShowPlayer.App/ShowWindow.xaml.cs:59`
- 现象：`TransitionTo()` 在 `_isTransitioning == true` 时直接 `return`。如果操作员在淡入淡出过程中连续点“下一个”或直接跳转，`PlaybackController` 和中控状态已经切换，但展示窗口会忽略后续项目。
- 风险：中控显示的当前播放项与大屏实际内容不一致，现场使用风险很高。
- 建议：不要简单丢弃转场请求。可保存 `_pendingItem`，当前动画结束后立即转到最后一次请求；或取消当前动画并切到新请求。`ItemStarted`、音乐播放和展示窗口内容加载最好由同一个状态机协调。

### 5. 图片音乐开始时机早于画面切入

- 位置：`src/ShowPlayer.App/ViewModels/MainViewModel.cs:294`、`src/ShowPlayer.App/ViewModels/MainViewModel.cs:308`、`src/ShowPlayer.App/ShowWindow.xaml.cs:59`
- 现象：`OnItemStarted()` 立即调用 `PlayMusicForCurrent()`，但 Fade 模式下图片要等黑场渐入完成后才 `LoadContent()`。
- 风险：切换图片时会先听到新音乐，画面仍处在旧内容或黑场，和“图片出现时音乐立即播放”不完全一致。
- 建议：让 `ShowWindow` 在内容实际加载完成后回调，或由播放控制器增加“内容已呈现”事件，再启动图片音乐淡入。

### 6. 全屏总是铺主屏，不能按展示窗口所在屏幕全屏

- 位置：`src/ShowPlayer.App/ShowWindow.xaml.cs:202`、`src/ShowPlayer.App/ShowWindow.xaml.cs:214`
- 现象：`EnterFullscreen()` 使用 `SystemParameters.PrimaryScreenWidth/Height`，并把 `Left/Top` 固定为 `0,0`。
- 风险：展示窗口拖到扩展屏后双击全屏，仍会回到主屏全屏，违背“拖拽至任意屏幕并全屏化”的核心需求。
- 建议：根据窗口当前矩形查找所在显示器，使用该屏幕的工作区或完整边界设置 `Left/Top/Width/Height`。WPF 可结合 `System.Windows.Forms.Screen.FromHandle(new WindowInteropHelper(this).Handle)` 实现。

## 中风险问题

### 7. 播放/暂停按钮文字容易和实际播放状态不同步

- 位置：`src/ShowPlayer.App/ViewModels/MainViewModel.cs:103`、`src/ShowPlayer.App/ViewModels/MainViewModel.cs:294`、`src/ShowPlayer.App/ShowWindow.xaml.cs:59`、`src/ShowPlayer.App/ShowWindow.xaml.cs:88`
- 现象：按钮文字依赖 VLC 的 `IsPlaying`，但属性通知发生在 `ItemStarted` 时；视频真正 `PlayVideo()` 要等转场加载内容，音乐也在通知之后才启动。
- 风险：正在播放时按钮可能仍显示“播放”，用户点击后执行暂停/继续逻辑会变得困惑。
- 建议：引入应用层播放状态，例如 `Playing/PauseRequested/Paused/Loading`，不要直接用 LibVLC 的瞬时 `IsPlaying` 驱动 UI；订阅 VLC `Playing/Paused/Stopped` 后回 UI 线程通知按钮状态。

### 8. 快捷键未设置 `e.Handled`，可能触发双重操作

- 位置：`src/ShowPlayer.App/MainWindow.xaml.cs:29`、`src/ShowPlayer.App/MainWindow.xaml.cs:32`、`src/ShowPlayer.App/ViewModels/MainViewModel.cs:501`
- 现象：`PreviewKeyDown` 调用快捷键逻辑后没有标记事件已处理。
- 风险：空格键可能既触发“下一个”，又触发当前焦点按钮/复选框的默认键盘行为；左右键也可能同时移动 Slider 然后执行快进/快退。
- 建议：`HandleKeyDown()` 返回 bool 表示是否处理，处理后设置 `e.Handled = true`。对 Slider、CheckBox、Button 等控件聚焦场景单独决定是否放行。

### 9. 展示窗口“最小化到任务栏”与当前配置矛盾

- 位置：`src/ShowPlayer.App/ShowWindow.xaml:9`、`src/ShowPlayer.App/ShowWindow.xaml.cs:219`、`src/ShowPlayer.App/ShowWindow.xaml.cs:240`
- 现象：需求要求展示窗口最小化到任务栏，但 XAML 和全屏进出逻辑都设置 `ShowInTaskbar = false`。
- 风险：展示窗口最小化后不出现在任务栏，只能靠中控按钮恢复；和需求、按钮提示“最小化到任务栏”不一致。
- 建议：窗口模式下 `ShowInTaskbar = true`，全屏时可按需要隐藏；退出全屏恢复窗口模式后也应恢复为 true。

### 10. 拖拽排序可能拖动的是之前选中的项目

- 位置：`src/ShowPlayer.App/MainWindow.xaml.cs:55`、`src/ShowPlayer.App/MainWindow.xaml.cs:75`
- 现象：拖拽开始时使用 `listBox.SelectedItem` 作为拖动对象。`PreviewMouseLeftButtonDown` 发生在 ListBox 更新选择之前，用户从未选中的行直接拖动时，可能拿到旧选中项。
- 风险：现场调整播放顺序时容易移动错误素材。
- 建议：在鼠标按下时从 `OriginalSource` 向上查找 `ListBoxItem`，以该容器的 `DataContext` 作为拖拽对象，并同步选中它。

### 11. 音乐文件缺失只记日志，不影响 UI 和播放逻辑

- 位置：`src/ShowPlayer.App/Services/FileValidator.cs:18`、`src/ShowPlayer.App/Services/PlaybackController.cs:156`
- 现象：背景音乐缺失时只记录日志，没有模型字段暴露给 UI；播放图片时仍会调用 `PlayMusic()`。
- 风险：操作员看不到具体哪条图片的背景音乐失效；播放时可能静默无声。
- 建议：给 `PlaylistItem` 增加 `IsMusicMissing`，右侧面板和列表提示音乐缺失；播放音乐前实时校验，不存在则跳过音乐并明确记录。

### 12. 退出时没有显式释放 VLC 资源

- 位置：`src/ShowPlayer.App/App.xaml.cs:12`、`src/ShowPlayer.App/Services/VlcMediaPlayer.cs:256`
- 现象：`VlcMediaPlayer` 实现了 `IDisposable`，但 `App` 没有 override `OnExit()` 调用 `_mediaPlayer.Dispose()`。
- 风险：正常退出大概率由进程回收兜底，但 VLC 原生资源、文件句柄和后台事件更适合明确释放；也利于后续自动化测试和热重启。
- 建议：在 `App.OnExit()` 中停止控制器、释放媒体播放器，并取消事件订阅。

## 交互与代码优化建议

### 13. 默认图片路径缺失时没有 UI 提示

- 位置：`src/ShowPlayer.App/ShowWindow.xaml.cs:121`
- 建议：设置默认闲置图片时先校验；停止播放加载失败时更新状态栏并写日志。当前 catch 后只清空图片，现场表现是黑屏，定位成本高。

### 14. 转场命名与行为建议统一

- 位置：`src/ShowPlayer.App/Models/AppSettings.cs:5`、`src/ShowPlayer.App/ShowWindow.xaml.cs:68`
- 现状：`TransitionType.Crossfade` 实际是“直接切换”，不是 crossfade。
- 建议：枚举改为 `FadeToBlack` / `Cut` 或 `Fade` / `Direct`，避免后续维护者误以为已有真正交叉淡化。

### 15. 发布/开发脚本会强制结束所有 dotnet 进程，范围过大

- 位置：`start.ps1`、`publish.ps1`
- 现象：脚本执行 `Get-Process -Name "ShowPlayer.App","MSBuild","VBCSCompiler","dotnet" | Stop-Process -Force`。
- 风险：会杀掉机器上其他项目的 `dotnet`、MSBuild 或编译服务。
- 建议：只结束当前应用进程，或按进程路径/命令行过滤到本项目；不要默认杀全局 `dotnet`。

### 16. 仓库里存在大量 `bin/obj` 构建产物，`.gitignore` 覆盖不足

- 位置：`.gitignore`
- 现象：当前 `.gitignore` 只忽略 `/debug.log` 和 `/ShowPlayer_v1.0.0.zip`，但源码树下包含大量 `bin`、`obj`、发布产物和 VLC 插件文件。
- 风险：仓库体积变大、diff 噪声严重，开发者难以审查真实代码变更。
- 建议：补充 `**/bin/`、`**/obj/`、`*.user`、`*.suo`、发布目录等忽略规则；已跟踪的构建产物需要单独从版本控制中移除。

### 17. 建议增加最小自动化测试覆盖核心状态机

- 当前项目没有测试工程。建议优先给 `PlaybackController`、`PlaylistManager`、`PlaylistPersistence` 增加单元测试，特别覆盖：
  - 缺失文件跳过逻辑；
  - 删除正在播放项后停止并显示默认图；
  - 拖拽排序后 `_currentIndex` 刷新；
  - 到达末尾后停止，再点 Next 从 0 开始；
  - 音乐循环和音量设置。

## 建议优先级

1. 先修复缺失文件跳过、音乐循环、音乐音量、转场期间快速切换、扩展屏全屏这 5 个问题，它们直接影响现场播放可靠性。
2. 再修复按钮状态、快捷键冒泡、拖拽误操作、任务栏显示等交互问题。
3. 最后处理资源释放、脚本范围、仓库忽略规则和测试工程，降低后续维护成本。

---

## 二次复核状态更新

原开发 AI 修复后，经二次复核发现部分边界问题。本轮已直接修复并同步记录到 `修复记录.md`：

- P1 缺失文件跳过：已修正末尾缺失项绕回开头的问题，保持“到末尾停止”的语义。
- P2 音乐循环：已避免在 VLC `EndReached` 回调线程里直接 Stop/Dispose/Play，并用 generation 防止停止后旧循环请求复活。
- P3 音乐音量：已在启动时应用 `settings.json` 中保存的音乐音量；淡入/淡出改为 double 步进，避免低音量下 Timer 卡住。
- P4 转场快速切换/停止竞态：已增加转场版本号和取消逻辑，`Stop()`、默认图加载会使旧动画回调失效。
- P5 图片音乐时机：已增加 `ContentLoaded` 与当前播放项一致性校验，避免快速切换时画面和音乐错配。
- M1 快捷键处理：左右键仅在视频播放时拦截，非视频场景放行给控件默认行为。
- M4 资源释放：展示窗口新增 `ForceClose()`，应用退出时绕过最小化保护，确保真正关闭。
- #14 转场命名：已兼容旧设置文件中的 `"Crossfade"` 并迁移为 `"Direct"`。
- 启动脚本构建失败：已修复 `start.ps1` 在删除 `obj/bin` 后从项目目录直接构建导致 WPF `*.g.cs` 生成文件缺失的问题；脚本现改为从仓库根目录构建 `ScreenProgram.sln`。`publish.ps1` 也同步改为从根目录指定项目路径执行 restore/publish。

仍建议后续处理：

- #11 音乐文件缺失 UI 提示仍未实现，当前主要依赖日志。
- #17 自动化测试仍未建立，建议优先覆盖播放状态机、缺失文件跳过、转场取消和音乐循环。
- `.gitignore` 已补规则，但历史已跟踪的 `bin/obj` 构建产物仍需从版本控制中移除。
